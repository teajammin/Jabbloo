/**
 * Shown on the launch screen, and sent with every crash report.
 *
 * A version in a log is only useful if it moves, so this is bumped with each
 * build handed to anyone else — the whole point of a report is knowing which
 * build produced it.
 */
export const VERSION = '0.9.0-rc.1';
