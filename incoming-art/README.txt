Drop replacement artwork here.

Name each file after the sprite it replaces and I will put it in the right
place, check it, and rebuild the sheet:

    bullet.png      -> public/effects/bullet.png
    hearts.png      -> public/effects/hearts.png
    A.png           -> public/letters/A.png
    placeholder-character-a.png -> public/placeholder-character-a.png

Any size, any format I can convert (png, jpg, webp, heic). Transparency is
kept where it exists. If a name does not match a sprite I will list it rather
than guess.

If the names do not match, drop them in anyway — I will show you each one
beside its filename so you can tell me which is which.
